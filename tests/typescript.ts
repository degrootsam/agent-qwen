// This is a sample TypeScript file with functions and types

function sayHello(name: string): string {
  return `Hello, ${name}!`;
}

function subtract(a: number, b: number): number {
  return a - b;
}

const divide = (x: number, y: number): number => x / y;
